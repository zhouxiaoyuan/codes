
server:
  port: 8089

spring:
  datasource:
    username: root
    password: 123456
    url: jdbc:mysql://172.16.17.52:3306/springboot?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=UTC
    driver-class-name: com.mysql.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapping/*Mapper.xml
  type-aliases-package: org.zxy.entity

#showSql
logging:
  level:
    com:
      example:
        mapper : debug

